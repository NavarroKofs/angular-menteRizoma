import { FormCurrencyModel } from './form-currency-model';

describe('FormCurrencyModel', () => {
  it('should create an instance', () => {
    expect(new FormCurrencyModel()).toBeTruthy();
  });
});
